dependencies {
	compile project(':core')
	compile project(':ease')
	compile project(':math')
}

test {
	useJUnitPlatform()

	dependsOn 'cleanTest'

	testLogging {
		events "passed", "skipped", "failed"
	}
}